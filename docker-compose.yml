version: "3.9"

services:
  restaurant_app:
    build: .
    container_name: restaurant_management
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - DATABASE_URL=sqlite:///./data/restaurant.db
    volumes:
      - ./data:/app/data
    restart: always